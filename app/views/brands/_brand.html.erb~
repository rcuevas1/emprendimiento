<li>
 <aside class="span2">
    <section>
      <h1><%= link_to brand.name, brand %> </h1>
    </section>
    <section>
        <% if brand.image? %>
		<%= image_tag brand.image.url %>		
	<%end%>
    </section> 
   
   <%if not current_user == nil%>
	<% if current_user.admin?%>
      | <%= link_to "delete", brand, method: :delete,
                    data: { confirm: "You sure?" } %>
  <% end %>
  <% end %>
  </aside>
 <aside class="span8">
	<% if brand.ideas.any? %>	
        <h3> <%= link_to "Related Ideas", brand%> </h3>
        <ol class="ideas"
  </br>
          
              <%= render 'ideas/idea',:idea => brand.ideas.first %>
         
        </ol>
        <%= will_paginate @ideas %>
    	<% end %>
 </aside>
</li>
