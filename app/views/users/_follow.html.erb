<%= form_for(current_user.followships.build(followed_idea_id: 1)) do |f| %>
    <div><%= f.hidden_field :followed_idea_id %></div>
    <%= f.submit "Follow", class: "btn btn-large btn-primary" %>
<% end %>