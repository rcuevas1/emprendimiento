 <%= form_for(Followship.find_by_followed_idea_id(idea.id),
                   html: { method: :delete }, remote: true) do |f| %>
          <%= f.submit "Jump Out", class: "btn btn-large" %>
 <% end %>
