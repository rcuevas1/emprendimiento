<li> 
<%if @popular_brands != nil%>
<%@popular_brands.each do |brand|%>
        <% if brand.image? %>	
	<%= link_to (image_tag brand.image.url(:thumb)), 
        url_for({:controller => 'brands', :action => 'show', :id => brand.id}) , :class => class="thumbnail"%>
	<%end%>
<%end%>
<%end%>
</li>
