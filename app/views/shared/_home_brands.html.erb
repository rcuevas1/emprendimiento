<li> 
<%if @popular_brands.length == 4%>
<% @popular_brands.each do |brand|%>
        <% if brand.image? %>
	
	<%= link_to (image_tag brand.image.url), 
        url_for({:controller => 'brands', :action => 'show', :id => brand.id})%>

	<%end%>
<%end%>
<%end%>
</li>
