<% if @popular_ideas.any? %>
    <ul class="ideas">
       <%@popular_ideas.each do |popular_idea|%>
     <li id="<%= popular_idea.id %>">  
     <span class="content"><h5><%=link_to popular_idea.content, url_for({:controller => 'ideas', :action => 'show', :id => popular_idea.id}) %></h5></span>    
    <span>
    Friends aboard:  <%= popular_idea.followers.count %>
   </span></br>  
   <span>   
   </li>
	<%end%>
    </ul>    
<% end %>
